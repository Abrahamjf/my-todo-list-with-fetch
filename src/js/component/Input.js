import React from "react";

export const Input = () => {
    return (
        <div className = "box">
            <input className ="tasks"placeholder="What needs to be done?"></input>
        </div>
    )
}